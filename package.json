{
  "name": "auto-microsoft-rewards",
  "version": "0.1.0",
  "description": "Extension Firefox pour automatiser Microsoft Rewards (recherches Bing, tâches quotidiennes, dashboard).",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "watch": "vite build --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9.1",
    "vite": "^5.4.10",
    "vite-plugin-web-extension": "^4.4.3",
    "vitest": "^2.1.5"
  },
  "dependencies": {
    "preact": "^10.24.3",
    "webextension-polyfill": "^0.12.0"
  }
}
